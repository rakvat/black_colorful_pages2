name: cache-events
on:
  schedule:
    - cron: "9,13,17,23,24,29,37,49,52 0-4 * * *"
  workflow_dispatch:

jobs:
  trigger-cache-events:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: curl https://schwarz-bunte-seiten-berlin.org/cache-events
